<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>User Profile</title>
  </head>
  <body class="bg-gradient-to-br from-zinc-800 via-zinc-900 to-black text-white font-sans">
    <a href="/" class="text-blue-600 text-center block my-4 absolute left-14"> Create User </a>
    <div class="min-h-screen flex flex-wrap items-center justify-center px-4 space-x-6">
      <% if(users.length === 0){ %>
      <h1 class="text-4xl font-bold text-center">No Users Found</h1>
      <% } else { %> <% users.forEach((user)=>{ %>
      <div class="userProfile bg-zinc-800 p-6 rounded-lg shadow-lg w-full max-w-sm">
        <div class="flex flex-col items-center justify-center">
          <img
            src="<%= user.imageUrl %>"
            alt="profile picture"
            class="rounded-full w-32 h-32 border-4 border-zinc-700 shadow-md object-cover"
          />
          <h1 class="text-2xl font-bold mt-4"><%= user.name %></h1>
          <p class="text-sm text-gray-400 mt-2"><%= user.email %></p>
        </div>
        <div class="mt-6 space-y-4">
          <a
            href="/edit/<%= user._id %>"
            class="w-full p-3 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-zinc-800 block text-center"
            >Edit Profile</a
          >
          <a
            href="/delete/<%= user._id %>"
            class="w-full p-3 bg-red-600 text-white font-semibold rounded-lg shadow-md hover:bg-red-700 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 focus:ring-offset-zinc-800 block text-center"
            >Delete Profile</a
          >
        </div>
      </div>
      <% }) %> <% } %>
    </div>
  </body>
</html>
